'use strict'

function OmniTile(){
	this.revision = 0;
	this.width = 1000;
	this.height = 1618;
}




/*
	//Create a I guess global thing?
	var OMNITILE = OMNITILE || {revision:0};
	
	OMNITILE.NewOmniTile() = function(){
		return new OmniTile();
	}

	OMNITILE.all = []; //Array of all OmniTiles that this OmniTile knows about.
	OmniTile.some = [];
	OmniTile.one = [];
	
	OmniTile.possible = [];
	OmniTile.now;
	
	
	OmniTile.makeMeTheMaster = function(please){
		if(please == true){
			if(OmniTile.IWorshipAnother == true){
				if()
				//Let the requester know that they will not be receiving the mastership
				return new OmniToken('I have another master');
			}
		}
	}
			
*/